# Local distribution builds

Run:

```bash
scripts/build-dist.sh
```

The script creates local Android, Web, and Desktop build artifacts under this folder. Generated binaries are ignored by git; tagged GitHub releases build and upload the release artifacts automatically.
